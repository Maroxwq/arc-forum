{% extends 'layout/two_columns.html.twig' %}

{% block title %}Edit Comment{% endblock %}

{% block content %}
    <h2 class="mb-4">Edit Comment</h2>
    {{ form_start(form) }}
        {{ form_row(form.content, {row_attr: { class: 'mb-3' }, attr: { rows: 4 }}) }}
        <button type="submit" class="btn btn-primary">Save Changes</button>
        <a href="{{ path('app_post_show', { id: comment.post.id }) }}" class="btn btn-link ms-3">Cancel</a>
    {{ form_end(form) }}
{% endblock %}
