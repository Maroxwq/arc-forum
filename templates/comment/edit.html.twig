{% extends 'layout/one_column.html.twig' %}

{% block title %}Edit Comment{% endblock %}

{% block content %}
    <h2 class="mb-4">Edit Comment</h2>
    {{ form_start(form) }}
        {{ form_row(form.content) }}
        <button type="submit" class="btn btn-primary">Save Changes</button>
        <a href="{{ path('app_post_show', { id: comment.post.id }) }}" class="btn btn-link ms-3">Cancel</a>
    {{ form_end(form) }}
{% endblock %}
