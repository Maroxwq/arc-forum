<button
    type="button"
    class="dropdown-item"
    data-action="confirm#open"
    data-confirm-url="{{ action }}"
    data-confirm-token="{{ csrf_token((token_intent|default('delete')) ~ id) }}">
    <i class="bi bi-trash me-2"></i>Delete
</button>
