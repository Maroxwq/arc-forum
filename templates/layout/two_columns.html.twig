{% extends 'layout/main.html.twig' %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col-3 mb-3">
                <div class="sticky-top" style="top: 96px;">
                    {% if app.user %}
                        <a href="{{ path('app_post_new') }}" class="btn btn-primary w-100 mb-4">Add Post</a>
                    {% endif %}

                    <h6>Tags</h6>
                    <ul class="list-group">
                        <li class="list-group-item">example</li>
                        <li class="list-group-item">example 123</li>
                    </ul>
                </div>
            </div>
                <div class="col-9">{% block content %}{% endblock %}</div>
        </div>
    </div>
{% endblock %}
