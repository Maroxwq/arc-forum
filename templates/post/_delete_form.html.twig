<form method="post" action="{{ action }}" onsubmit="return confirm('Delete?');">
    <input type="hidden" name="_token" value="{{ csrf_token((token_intent|default('delete')) ~ id) }}">
    <button type="submit" class="dropdown-item"><i class="bi bi-trash me-2"></i>Delete</button>
</form>
